
// This file is no longer used as AppLayout and TopActionsBar handle its previous responsibilities.
// It can be deleted or kept for reference if PDF report generation is re-introduced later.
// For now, making it an empty export to avoid breaking imports if any linger.

export {};
